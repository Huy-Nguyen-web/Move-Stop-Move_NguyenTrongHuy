using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HitArea : MonoBehaviour
{
    // [SerializeField] private Character parentCharacter;
    // private void OnTriggerEnter(Collider other) {
    //     if(other.gameObject != parentCharacter.gameObject){
    //         parentCharacter.enemyInRange.Add(other.gameObject);
    //         parentCharacter.UpdateCurrentTarget();
    //     }
    // }
    // private void OnTriggerExit(Collider other) {
    //     parentCharacter.enemyInRange.Remove(other.gameObject);
    //     parentCharacter.UpdateCurrentTarget();
    // }
}
